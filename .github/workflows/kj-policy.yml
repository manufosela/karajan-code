# >>> kj:managed:wf-policy v1 >>> (do not edit: regenerated by kj harden)
name: Policy
on:
  pull_request:
permissions:
  contents: read
jobs:
  policy-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
      - name: kj policy check (strict) on the PR diff
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          if [ -f bin/kj.js ]; then npm ci --ignore-scripts && KJ="node bin/kj.js"; else KJ="npx --yes karajan-code@4.27.0"; fi
          $KJ policy check --range "origin/${BASE_REF}...HEAD" --strict

# <<< kj:managed:wf-policy <<<
