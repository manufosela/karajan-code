# >>> kj:managed:wf-pack-smoke v1 >>> (do not edit: regenerated by kj harden)
name: Pack smoke
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  pack-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
      - run: npm ci
      - name: Pack and install the tarball clean
        run: |
          tgz=$(npm pack --silent)
          dir=$(mktemp -d)
          npm install --prefix "$dir" "$PWD/$tgz" --no-audit --no-fund
          echo "Tarball installs clean: $tgz"

# <<< kj:managed:wf-pack-smoke <<<
