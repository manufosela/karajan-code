services:
  hu-board:
    build: .
    ports:
      - "${HU_BOARD_PORT:-4000}:4000"
    volumes:
      - ${KJ_HOME:-~/.karajan}:/data
    environment:
      - KJ_HOME=/data
      - HU_BOARD_TOKEN=${HU_BOARD_TOKEN:-}
    restart: unless-stopped
