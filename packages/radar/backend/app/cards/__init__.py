"""Distilled cards: the shape this radar emits for the karajan family corpus.

A card is a document an agent can decide from without reading the source it
came from. The format is not ours: it is the one karajan-code's ``library/``
corpus already consumes, and this package follows it rather than the reverse.
"""
