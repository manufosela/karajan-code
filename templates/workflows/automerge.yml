# Auto-merge PRs approved by Karajan CI reviewer.
# Merges when kj-reviewer[bot] approves and all checks pass.

name: Auto-merge (Karajan CI)

on:
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    if: >
      github.event.review.state == 'approved' &&
      contains(github.event.review.user.login, 'kj-reviewer')
    runs-on: ubuntu-latest
    steps:
      - name: Enable auto-merge
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              merge_method: 'squash'
            });
